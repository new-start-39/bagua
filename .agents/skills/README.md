# Project skills

Reserved for reusable task procedures. Do not add skills until the workflow is understood from real project work.

Each skill should live in `.agents/skills/<name>/SKILL.md` and document:

- when it should be used;
- required context and inputs;
- the procedure;
- commands and checks;
- definition of done.
