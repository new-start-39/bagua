# Harness configuration

This directory contains machine-readable settings for the repository harness. Keep policy and explanation in `AGENTS.md`, `ARCHITECTURE.md`, and `docs/`; keep this file focused on stable paths and verification entry points.
